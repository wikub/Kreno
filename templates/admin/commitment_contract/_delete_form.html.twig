<form method="post" action="{{ path('commitment_contract_delete', {'id': commitment_contract.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commitment_contract.id) }}">
    <button class="btn">Delete</button>
</form>
