<form method="post" action="{{ path('admin_param_delete', {'id': param.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ param.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
