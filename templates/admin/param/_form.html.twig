{% form_theme form '_form/_form_theme.html.twig' %}

{{ form_start(form) }}
    {{ form_widget(form) }}
    
    <div class="text-end mt-3">
        <div class="btn-group">
            <a href="{{ path('admin_param_index') }}" class="btn btn-secondary"><i class="fas fa-list"></i> retour</a>
            <button class="btn btn-primary"><i class="far fa-save"></i> {{ button_label|default('Enregistrer') }}</button>
        </div>
    </div>
{{ form_end(form) }}
