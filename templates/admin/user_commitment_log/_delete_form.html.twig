<form method="post" action="{{ path('admin_user_commitment_log_delete', {'id': commitment_log.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commitment_log.id) }}">
    <button class="btn">Delete</button>
</form>
