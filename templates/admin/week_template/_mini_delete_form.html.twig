<form method="post" action="{{ path('admin_week_template_delete', {'id': week_template.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');"
    style="display: inline"
>
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ week_template.id) }}">
    <button class="btn btn-link"><i class="far fa-trash-alt"></i></button>
</form>
