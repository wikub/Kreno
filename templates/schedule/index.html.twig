{% extends 'base.html.twig' %}

{% block title %}Hello PlanningController!{% endblock %}

{% block body %}
<div class="pt-3 pb-2 mb-3 border-bottom">
    <h1 class="h2"><i class="fas fa-calendar-alt"></i> Planning</h1>
</div>

<div>
    <div class="row align-items-start mb-3">
        {% for day in dayOfWeek %}
        <div class="col text-center">
            {{ day.date|date('l d/m') }}
        </div>
        {% endfor %}
    </div>
    <div class="row align-items-start">
        {% for day in dayOfWeek %}
        <div class="col">
            {% for timeslot in day.timeslots %}
                <div class="card mb-3">
                    <div class="card-header">
                        {{ timeslot.name }}
                    </div>
                    <div class="card-body">
                        <p class="card-text">
                            {{ timeslot.start|date('H:i') }} à {{ timeslot.finish|date('H:i') }}
                        </p>
                    </div>
                    <ul class="list-group list-group-flush">
                    <li class="list-group-item {{ timeslot.manager?'bg-primary':'bg-warning'}} ">
                        <i class="fas fa-user-tie"></i> {{ timeslot.manager?timeslot.manager.firstname:'libre' }} {{ timeslot.manager?timeslot.manager.name }}
                    </li>
                    {% for job in timeslot.jobs %}
                        <li class="list-group-item {{ job.user?'bg-primary':'bg-warning'}} ">
                            {% if job.user %}
                            {{job.user.firstname}} {{job.user.name}}
                            {% else %}
                            libre
                            {% endif %}
                        </li>
                    {% endfor %}

                    </ul>
                </div>
            {% endfor %}
        </div>
        {% endfor %}
    </div>

</div>
{% endblock %}
