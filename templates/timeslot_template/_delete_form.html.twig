<form method="post" action="{{ path('timeslot_template_delete', {'id': timeslot_template.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ timeslot_template.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
